create table departments(
                         dept_id         number(3),
                         dept_name       varchar2(50),
                         dept_sta_bonus  varchar2(1),
              constraint pk_dept
                 primary key(dept_id)
                        );
